<h1><%= pluralize(@users.length, 'User') %></h1>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <h3><%= link_to user.name, user %></h3>
      <p>
        Created <%= distance_of_time_in_words_to_now(user.created_at) %> ago
      </p>
    </li>
  <% end %>
</ul>
